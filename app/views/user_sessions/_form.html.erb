<%= form_for(@user_session) do |f| %>

  <% if @user_session.errors.any? %>
    <div id="error_explanation">
      <h4><%= pluralize(@user_session.errors.count, "error") %> prohibited this user_session from being saved:</h4>

      <ul>
      <% @user_session.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nick %>
    <%= f.text_field :nick %>
  </div>
  <div class="field">
    <%= f.label :password %>
    <%= f.password_field :password %>
  </div>
  <br/>
  <div class="actions">
	<button type="submit" class="btn btn-primary">Login</button>
  </div>
<% end %>
